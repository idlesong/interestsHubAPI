<%= form_for(current_user.subscriptions.build) do |f| %>
  <div> <%= hidden_field_tag :tag_id, tag.id %> </div>
  <%= tag.tag %> <%= f.submit "subscribe", class: "btn btn-primary btn-xs" %>
<% end %>
